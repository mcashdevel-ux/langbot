# Dev/test dependencies. Installs the runtime deps too so the full test suite
# (including tests that import components.vault, which needs `cryptography`)
# can be collected and run.
-r requirements.txt

# Testing
pytest>=8,<10
pytest-cov>=5

# Linting & Formatting
ruff
black

# Security
pip-audit
